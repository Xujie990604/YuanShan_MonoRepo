# excludeDependencies

## 一、使用 external 排除依赖

1. vite 配置
2. 都有哪些依赖可以使用 CDN 加速或者 本地加载的形式？有哪些特征？要求？
3. 排除多个依赖时，多个依赖的加载顺序会不会有要求。这个依赖的加载顺序一定要在 index.html 中手写吗？有其他的好办法吗？
4. 增加排除项， Vue， ant-design-vue， axios, dayjs 等
5. 目前 vant 配置了，但是并没有真的被排除。不知道为何
6. vite-plugin-external  看着这个新插件能有什么新的作用